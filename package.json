{
  "name": "platformatic-db-basic",
  "scripts": {
    "dev": "platformatic start",
    "clean": "rimraf **/dist --glob",
    "docker:build": "docker build -t platformatic-db-basic .",
    "docker:build-nocache": "docker build --progress=plain --no-cache -t platformatic-db-basic .",
    "build": "platformatic compile"
  },
  "devDependencies": {
    "fastify": "^5.0.0",
    "borp": "^0.17.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@platformatic/runtime": "^2.5.2",
    "platformatic": "^2.5.2"
  }
}
